@model IEnumerable<SupportInvestigation.Models.Model.Marchand>

@{
    ViewBag.Title = "ListMarchand";
}

<h2>ListMarchand</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Name
        </th>
        <th>
            Url
        </th>
        <th>
            ContactName
        </th>
        <th>
            Phone
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Url)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MarchandID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MarchandID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MarchandID })
        </td>
    </tr>
}

</table>

<br />

@Html.ActionLink("Retourner à la page d'adminsitration", "Home")